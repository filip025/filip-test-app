import { styled } from "@mui/system";
import Button from "@mui/material/Button";

const PrimaryBtn = styled(Button)({
  textTransform: "capitalize",
  fontWeight: 400,
  fontStyle: "italic",
  padding: "10px 15px",
});

const ButtonStyle = { PrimaryBtn };

export default ButtonStyle;
